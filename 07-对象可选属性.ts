// const myAxios: (config: {url: string; methods?: string}) => void = (config) => {
//   console.log(config);
// }

const myAxios = (config: {url: string; methods?: string}): void => {
  console.log(config);
}
export {}